import { FileNamePipePipe } from './file-name-pipe.pipe';

describe('FileNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FileNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
